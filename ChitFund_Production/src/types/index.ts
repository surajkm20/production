// Shared TypeScript types and interfaces used across multiple layers.
// Examples: PaginatedResponse<T>, ApiErrorCode (union of all error code strings from api.md §12),
// BasketTxnType, LoanStatus, MemberRole — typed mirrors of DB enum-like string columns.
